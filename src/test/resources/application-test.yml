# Use in-memory H2 database for unit tests
spring:
  datasource:
    jdbc-url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driverClassName: org.h2.Driver
    connection-test-query: select 1
    maximum-pool-size: 5
    minimum-idle: 2
    pool-name: SpringSecurityPoolH2
